package android.content;

import android.os.IInterface;

/**
 * 编译期 stub：android.content.IContentProvider 是隐藏类，不在 android.jar 中。
 * 仅用于让 FakeContext 的 ContentResolver 匿名子类通过 javac 编译；
 * 运行时设备上有真实类，此 stub 不进 dex（build.ps1 只把 src/ 产物给 d8）。
 */
public interface IContentProvider extends IInterface {
}
